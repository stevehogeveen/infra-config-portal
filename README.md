# infra-config-portal workspace

This folder is a workspace container.

- `app/` contains the current infra-config-portal MVP scaffold.
- `reference/` is reserved for older notes, experiments, or adjacent app ideas
  that can be consulted while building the portal.

Run the app from `app/`.
